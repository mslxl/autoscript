
fn main(){
    val num = 3 * 4 + 1;
    var ww = 3 * (1+2);
    return num * ww / num;
}

fn add(a:int, b:int) -> int{
   val result = 1 + 3;
   return result;
}

fn add(a:float, b:float) -> float{
   return 3-1.0;
}